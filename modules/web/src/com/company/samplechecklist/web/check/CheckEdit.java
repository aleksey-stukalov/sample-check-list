package com.company.samplechecklist.web.check;

import com.company.samplechecklist.entity.Check;
import com.haulmont.cuba.gui.components.AbstractEditor;

public class CheckEdit extends AbstractEditor<Check> {
    /*
    @Inject
    private FieldGroup fieldGroup;

    @Override
    public void init(Map<String, Object> params) {
        super.init(params);

        LookupPickerField field = (LookupPickerField) fieldGroup.getField("optionsGroup").getComponent();
        field.getLookupAction().setLookupScreenParams(ParamsMap.of(OptionsGroupEdit.COMMAND_SHOW_OPTIONS_EDITOR, false));
    }
    */

}